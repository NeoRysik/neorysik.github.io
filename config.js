/* Public configuration only. Never place API keys, passwords, or tokens here. */
window.SITE_CONFIG = {
  // Paste the HTTPS form action from MailerLite's embedded form code.
  // Example shape: https://assets.mailerlite.com/jsonp/ACCOUNT/forms/FORM/subscribe
  mailerLiteFormAction: ""
};
