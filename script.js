const menu=document.querySelector('.menu'),links=document.querySelector('.nav-links');menu?.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});links?.addEventListener('click',()=>{links.classList.remove('open');menu?.setAttribute('aria-expanded','false')});document.querySelector('#year').textContent=new Date().getFullYear();
const form=document.querySelector('[data-mailerlite-form]');const status=form?.querySelector('.form-status');const endpoint=window.SITE_CONFIG?.mailerLiteFormAction;
if(form&&endpoint){form.action=endpoint;form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='Sending…';const button=form.querySelector('button');button.disabled=true;try{const response=await fetch(endpoint,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Unable to subscribe');form.reset();status.textContent='Thank you. Please check your inbox to confirm and receive the chapters.'}catch(error){status.textContent='We could not complete the signup. Please try again shortly.'}finally{button.disabled=false}})}else if(form){form.addEventListener('submit',e=>{e.preventDefault();status.textContent='Newsletter signup is being connected now. Please return shortly or email neorysik@gmail.com.'})}
